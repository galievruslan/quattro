<h1>Editing contact</h1>

<%= form_for @Contact, url: administration_contact_path(@Contact) do |f| %>
  <% if @Contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@Contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @Contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit(:class =>'btn') %>
  </div>
<% end %>

<%= link_to 'Back', edit_administration_company_path(1) %>