<%a=0%>
<% @projects.each do |project| %>
  <% if (!project.nil? && project.project_image)%>
    <% if a==0%>
      <a href="#"><%= image_tag(project.project_image.thumb('330x330#').url, :id=>"startAtMe")%></a>
      <%a=1%>
    <% else %>
    <a href="#"><%= image_tag(project.project_image.thumb('330x330#').url)%></a>
    <% end %>
  <% end %>
<% end %>